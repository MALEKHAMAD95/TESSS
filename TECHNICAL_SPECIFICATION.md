<div dir="rtl">

# 🔧 المواصفات التقنية التفصيلية - نظام إدارة العيادات المتكامل
## دليل بناء النظام من الصفر لـ AI Agent

---

## 📋 نظرة عامة على النظام

### معلومات أساسية
- **اسم النظام:** نظام إدارة العيادات المتكامل
- **السنة:** © 2026
- **الرابط:** https://crm-clincks.onrender.com
- **اللغة:** العربية (RTL - من اليمين لليسار)
- **نوع التطبيق:** Web Application - Single Page Application (SPA)

### التقنيات المقترحة
- **Frontend:** React.js / Next.js مع TypeScript
- **UI Framework:** Tailwind CSS أو Chakra UI
- **State Management:** Redux أو Zustand
- **Backend:** Node.js مع Express أو NestJS
- **Database:** PostgreSQL أو MongoDB
- **Authentication:** JWT (JSON Web Tokens)
- **Hosting:** Render.com

---

## 🎨 نظام الألوان والتصميم

### الألوان الرئيسية
```css
:root {
  /* اللون الأساسي - ذهبي/أصفر */
  --primary-color: #F5A623;
  --primary-hover: #E09000;
  --primary-light: #FFF8E7;
  
  /* اللون الثانوي - أخضر */
  --success-color: #4CAF50;
  --success-light: #E8F5E9;
  
  /* ألوان الحالات */
  --danger-color: #F44336;
  --warning-color: #FF9800;
  --info-color: #2196F3;
  
  /* ألوان الخلفية */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F5F5F5;
  --bg-sidebar: #1A1A2E;
  
  /* ألوان النصوص */
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-light: #999999;
  --text-white: #FFFFFF;
  
  /* الحدود */
  --border-color: #E0E0E0;
  --border-radius: 8px;
  --border-radius-lg: 12px;
}
```

### الخطوط
```css
body {
  font-family: 'Cairo', 'Tajawal', sans-serif;
  direction: rtl;
}
```

---

## 🏗️ هيكل التطبيق

### البنية العامة للصفحات
```
┌─────────────────────────────────────────────────────────────┐
│                     Header (الشريط العلوي)                   │
│  [Logo] [اسم المستخدم] [اختيار الفرع] [🔔] [🌙] [تسجيل خروج] │
├──────────────┬──────────────────────────────────────────────┤
│              │                                               │
│   Sidebar    │              Main Content                     │
│   (القائمة    │              (المحتوى الرئيسي)                │
│   الجانبية)   │                                               │
│              │                                               │
│  - لوحة التحكم │                                               │
│  - الفروع     │                                               │
│  - الموظفين   │                                               │
│  - الأطباء    │                                               │
│  - المرضى     │                                               │
│  - المنتجات   │                                               │
│  - الباكجات   │                                               │
│  - الحجوزات   │                                               │
│  - الفواتير   │                                               │
│  - الإيصالات  │                                               │
│  - فواتير     │                                               │
│    المنتجات   │                                               │
│  - التقارير   │                                               │
│  - الملف      │                                               │
│    الشخصي    │                                               │
│  - الإعدادات  │                                               │
│              │                                               │
└──────────────┴──────────────────────────────────────────────┘
```

---

## 📱 الشاشات والمكونات التفصيلية

---

# 1️⃣ صفحة تسجيل الدخول (Login Page)

### الوصف
صفحة بسيطة لتسجيل الدخول للنظام

### المكونات
```
┌─────────────────────────────────────┐
│                                     │
│           [شعار النظام]              │
│                                     │
│     ┌─────────────────────────┐     │
│     │     اسم المستخدم        │     │
│     └─────────────────────────┘     │
│                                     │
│     ┌─────────────────────────┐     │
│     │     كلمة المرور 👁️      │     │
│     └─────────────────────────┘     │
│                                     │
│     ☐ تذكرني                        │
│                                     │
│     ┌─────────────────────────┐     │
│     │      تسجيل الدخول       │     │
│     └─────────────────────────┘     │
│                                     │
│         نسيت كلمة المرور؟           │
│                                     │
└─────────────────────────────────────┘
```

### الحقول
| الحقل | النوع | مطلوب | التحقق |
|-------|-------|-------|--------|
| username | text | ✅ | min: 3 chars |
| password | password | ✅ | min: 6 chars |
| rememberMe | checkbox | ❌ | - |

### API Endpoints
```
POST /api/auth/login
Body: { username: string, password: string }
Response: { token: string, user: UserObject }
```

---

# 2️⃣ الشريط العلوي (Header Component)

### الوصف
شريط ثابت في أعلى الصفحة يظهر في جميع الصفحات بعد تسجيل الدخول

### المكونات (من اليمين لليسار)
```
┌────────────────────────────────────────────────────────────────────┐
│ [↩️] │ Admin_system │ [🔔] │ [🌙] │ [🏢 جميع الفروع ▼] │ [<]  │ نظام الإدارة │
│      │   مدير النظام  │      │      │                    │      │              │
└────────────────────────────────────────────────────────────────────┘
```

### العناصر
1. **زر تسجيل الخروج** (أيقونة ↩️ حمراء)
2. **معلومات المستخدم:**
   - اسم المستخدم (Admin_system)
   - الدور (مدير النظام)
3. **أيقونة الإشعارات** 🔔
4. **زر تبديل الوضع** (فاتح/داكن) 🌙
5. **اختيار الفرع:** Dropdown لاختيار الفرع
6. **زر طي/فتح القائمة الجانبية** (<)
7. **عنوان النظام:** "نظام الإدارة"

---

# 3️⃣ القائمة الجانبية (Sidebar Component)

### الوصف
قائمة تنقل جانبية قابلة للطي

### العناصر
```javascript
const menuItems = [
  { id: 1, icon: "grid", label: "لوحة التحكم", path: "/dashboard" },
  { id: 2, icon: "building", label: "الفروع", path: "/branches" },
  { id: 3, icon: "users", label: "الموظفين", path: "/employees" },
  { id: 4, icon: "stethoscope", label: "الأطباء", path: "/doctors" },
  { id: 5, icon: "user-injured", label: "المرضى", path: "/patients" },
  { id: 6, icon: "box", label: "المنتجات والمخزون", path: "/products" },
  { id: 7, icon: "layers", label: "الباكجات / الخدمات", path: "/packages" },
  { id: 8, icon: "calendar", label: "الحجوزات", path: "/appointments" },
  { id: 9, icon: "file-invoice", label: "الفواتير", path: "/invoices" },
  { id: 10, icon: "receipt", label: "إيصالات العملاء", path: "/receipts" },
  { id: 11, icon: "shopping-cart", label: "فواتير المنتجات", path: "/product-invoices" },
  { id: 12, icon: "chart-bar", label: "التقارير المالية", path: "/reports" },
  { id: 13, icon: "user", label: "الملف الشخصي", path: "/profile" },
  { id: 14, icon: "cog", label: "إعدادات النظام", path: "/settings" }
];
```

### التصميم
- الخلفية: داكنة (#1A1A2E)
- العنصر النشط: خلفية صفراء/ذهبية
- الأيقونات: بيضاء
- النص: أبيض
- عند التحويم: تغيير لون الخلفية

---

# 4️⃣ لوحة التحكم الرئيسية (Dashboard)

### الوصف
الصفحة الرئيسية بعد تسجيل الدخول - تعرض ملخص شامل للنظام

### المكونات

#### أ) بطاقات الإحصائيات (Stats Cards)
```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 👥           │ │ 🩺           │ │ 📅           │ │ 📦           │ │ 💰           │
│ إجمالي المرضى │ │ الأطباء      │ │ حجوزات اليوم │ │ المنتجات     │ │ إيرادات الشهر │
│     6        │ │     4        │ │     0        │ │     5        │ │   14,500     │
│ إجمالي المسجلين│ │ الطاقم الطبي │ │ 0 موعد لليوم │ │ منتج مسجل   │ │ الوقية موريتاني│
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

##### بنية البطاقة:
```typescript
interface StatCard {
  icon: string;
  iconColor: string;
  title: string;
  value: number | string;
  subtitle: string;
  trend?: {
    direction: 'up' | 'down';
    percentage: number;
  };
}
```

#### ب) الإجراءات السريعة (Quick Actions)
```
┌─────────────────────────────────┐
│        إجراءات سريعة            │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ 📅 حجز جديد                 │ │ ← زر أصفر/ذهبي
│ │    إضافة موعد جديد          │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 👤 مريض جديد                │ │ ← زر أخضر
│ │    تسجيل مريض               │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 📦 إضافة منتج               │ │ ← زر أخضر فاتح
│ │    منتج أو خدمة             │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### ج) قسم الحجوزات مع التقويم
```
┌─────────────────────────────────────────────────────┐
│                    الحجوزات                          │
├─────────────────────────────────────────────────────┤
│            📅 6 فبراير 2026                          │
├─────────────────────────────────────────────────────┤
│  < │ سبت  │ أحد │ اثنين │ ثلاثاء │ أربعاء │ خميس │ جمعة │ > │
│    │  31  │  1  │   2   │   3   │   4   │  5   │ [6] │   │
│    │      │ 1   │       │       │   1   │  2   │     │   │
└─────────────────────────────────────────────────────┘
│                                                      │
│              🗓️ لا توجد حجوزات                       │
│           لا توجد حجوزات في هذا اليوم                │
│                                                      │
│            [عرض جميع الحجوزات]                       │
└─────────────────────────────────────────────────────┘
```

#### د) آخر المرضى (Recent Patients)
```
┌─────────────────────────────────────────────────────┐
│                   آخر المرضى                         │
├─────────────────────────────────────────────────────┤
│ [مع] محمد عبدالرحمن          📞 0512345678   🕐 اليوم  │
│ [سا] سارة الأحمد             📞 0523456789   🕐 أمس    │
│ [خا] خالد العتيبي            📞 0534567890   🕐 منذ يومين│
│ [نو] نورة السالم             📞 0545678901   🕐 منذ 3 أيام│
├─────────────────────────────────────────────────────┤
│              [عرض جميع المرضى]                       │
└─────────────────────────────────────────────────────┘
```

---

# 5️⃣ إدارة الفروع (Branches Management)

## 5.1 صفحة قائمة الفروع

### العنوان
```
إدارة الفروع
عرض وإدارة جميع فروع الشركة (3 فرع)
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ إضافة فرع جديد]                    🔍 البحث عن فرع بالاسم أو العنوان...│
└────────────────────────────────────────────────────────────────┘
```

### بطاقات الفروع (Cards Layout)
```
┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────────────────┐
│ ⋮  [🏢] medox    [نشط] │  │ ⋮  [🏢] فرع جديد  [نشط] │  │ ⋮  [🏢] hjkhjkhjkh [نشط]│
├─────────────────────────┤  ├─────────────────────────┤  ├─────────────────────────┤
│ 📍 testd45dd            │  │ 📍 عنوان                │  │ 📍 jkhjkhjkhjkhjk       │
│ 📞 01015485694          │  │ 📞 01017404798          │  │ 📞 01026786173          │
├─────────────────────────┤  ├─────────────────────────┤  ├─────────────────────────┤
│ 👥 الموظفين: 0  📈 المبيعات: 0│  │ 👥 الموظفين: 0  📈 المبيعات: 0│  │ 👥 الموظفين: 0  📈 المبيعات: 0│
├─────────────────────────┤  ├─────────────────────────┤  ├─────────────────────────┤
│ الأداء: 0% ━━━━━━━━━━   │  │ الأداء: 0% ━━━━━━━━━━   │  │ الأداء: 0% ━━━━━━━━━━   │
├─────────────────────────┤  ├─────────────────────────┤  ├─────────────────────────┤
│ المدير المسؤول: غير محدد │  │ المدير المسؤول: غير محدد │  │ المدير المسؤول: غير محدد │
└─────────────────────────┘  └─────────────────────────┘  └─────────────────────────┘
```

### قائمة الإجراءات (⋮ Menu)
```
┌──────────┐
│ ✏️ تعديل │
│ 🗑️ حذف  │
└──────────┘
```

## 5.2 نموذج إضافة/تعديل فرع

### العنوان
```
→ إضافة فرع جديد
   أدخل بيانات الفرع الجديد
```

### النموذج
```
┌─────────────────────────────────────────────────────┐
│           ┌─────────────────────┐                    │
│           │    [🏢 أيقونة]       │  ← خلفية ذهبية    │
│           └─────────────────────┘                    │
│                                                      │
│                  معلومات الفرع                       │
│            أدخل البيانات الأساسية للفرع              │
│                                                      │
│  🏢 اسم الفرع                                        │
│  ┌─────────────────────────────────────────────┐    │
│  │                                             │    │
│  └─────────────────────────────────────────────┘    │
│                                                      │
│  📍 عنوان الفرع                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │                                             │    │
│  └─────────────────────────────────────────────┘    │
│                                                      │
│  📞 رقم الهاتف                                       │
│  ┌─────────────────────────────────────────────┐    │
│  │                                             │    │
│  └─────────────────────────────────────────────┘    │
│                                                      │
│  ┌─────────────────────┐  ┌─────────────────────┐   │
│  │     ✖️ إلغاء        │  │   💾 حفظ الفرع      │   │
│  └─────────────────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب | التحقق |
|-------|-------------------|-------|-------|--------|
| اسم الفرع | name | text | ✅ | min: 2 chars |
| عنوان الفرع | address | text | ✅ | min: 5 chars |
| رقم الهاتف | phone | tel | ✅ | valid phone |

### API Endpoints
```
GET    /api/branches              - قائمة الفروع
GET    /api/branches/:id          - فرع واحد
POST   /api/branches              - إضافة فرع
PUT    /api/branches/:id          - تعديل فرع
DELETE /api/branches/:id          - حذف فرع
```

### Database Schema
```sql
CREATE TABLE branches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  address VARCHAR(255) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  manager_id UUID REFERENCES employees(id),
  status ENUM('active', 'inactive') DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 6️⃣ إدارة الموظفين (Employees Management)

## 6.1 صفحة قائمة الموظفين

### العنوان
```
إدارة الموظفين
إدارة بيانات الموظفين وتتبع أدائهم
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ إضافة موظف]  [⬇️ تصدير]          🔍 البحث بالاسم أو اسم المستخدم...│
└────────────────────────────────────────────────────────────────┘
```

### بطاقات الإحصائيات
```
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│    👥 3          │  │    ✅ 3          │  │    ❌ 0          │
│ إجمالي موظفي الفرع │  │   نشط حالياً     │  │    غير نشط       │
└──────────────────┘  └──────────────────┘  └──────────────────┘
```

### فلاتر
```
┌───────┐ ┌───────┐ ┌──────────┐
│ [الكل] │ │ نشط  │ │ غير نشط │
└───────┘ └───────┘ └──────────┘
```

### جدول الموظفين
```
┌─────────────────────────────────────────────────────────────────────────────┐
│ الموظف              │ المنصب │ الفرع              │ الحالة │ تاريخ الانضمام │ ⋮ │
├─────────────────────────────────────────────────────────────────────────────┤
│ [صورة] Nael         │ SALES │ 🏢 فرع جديد        │ [نشط] │ 2026/2/4      │ ⋮ │
│        Nael@        │       │                    │       │               │   │
├─────────────────────────────────────────────────────────────────────────────┤
│ [صورة] Abdelrahman  │ SALES │ 🏢 hjkhjkhjkh...   │ [نشط] │ 2026/1/14     │ ⋮ │
│        Abdaal@      │       │                    │       │               │   │
├─────────────────────────────────────────────────────────────────────────────┤
│ [صورة] Admin_system │ ADMIN │ 🏢 بدون فرع        │ [نشط] │ 2026/1/11     │ ⋮ │
│        Admin@       │       │                    │       │               │   │
├─────────────────────────────────────────────────────────────────────────────┤
│                        عرض 3 موظف من إجمالي 3                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### قائمة الإجراءات (⋮ Menu)
```
┌──────────┐
│ ✏️ تعديل │
│ 🗑️ حذف  │
└──────────┘
```

## 6.2 نموذج إضافة/تعديل موظف

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| الاسم الكامل | fullName | text | ✅ |
| اسم المستخدم | username | text | ✅ |
| البريد الإلكتروني | email | email | ❌ |
| كلمة المرور | password | password | ✅ (عند الإضافة) |
| رقم الهاتف | phone | tel | ❌ |
| المنصب | role | select | ✅ |
| الفرع | branchId | select | ✅ |
| الحالة | status | select | ✅ |

### خيارات المنصب (Role)
```javascript
const roles = [
  { value: 'ADMIN', label: 'مدير النظام' },
  { value: 'SALES', label: 'موظف مبيعات' },
  { value: 'DOCTOR', label: 'طبيب' },
  { value: 'RECEPTIONIST', label: 'موظف استقبال' }
];
```

### API Endpoints
```
GET    /api/employees              - قائمة الموظفين
GET    /api/employees/:id          - موظف واحد
POST   /api/employees              - إضافة موظف
PUT    /api/employees/:id          - تعديل موظف
DELETE /api/employees/:id          - حذف موظف
```

### Database Schema
```sql
CREATE TABLE employees (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(100),
  password_hash VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  role ENUM('ADMIN', 'SALES', 'DOCTOR', 'RECEPTIONIST') NOT NULL,
  branch_id UUID REFERENCES branches(id),
  status ENUM('active', 'inactive') DEFAULT 'active',
  avatar_url VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 7️⃣ إدارة الأطباء (Doctors Management)

## 7.1 صفحة قائمة الأطباء

### العنوان
```
إدارة الأطباء
إدارة بيانات الأطباء والطاقم الطبي
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ إضافة طبيب]  [⬇️ تصدير]          🔍 البحث بالاسم أو التخصص...│
└────────────────────────────────────────────────────────────────┘
```

### بطاقات الإحصائيات
```
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│    🩺 4          │  │    ✅ 4          │  │    ❌ 0          │
│  إجمالي الأطباء  │  │   نشط حالياً     │  │    غير نشط       │
└──────────────────┘  └──────────────────┘  └──────────────────┘
```

### جدول الأطباء
```
┌───────────────────────────────────────────────────────────────────────────────────┐
│ الطبيب           │ التخصص      │ الهاتف       │ الفرع      │ الحالة │ تاريخ الانضمام │
├───────────────────────────────────────────────────────────────────────────────────┤
│ [صورة] د. أحمد   │ جراحة عامة  │ 0512345678  │ 🏢 فرع 1   │ [نشط] │ 2026/1/15     │
├───────────────────────────────────────────────────────────────────────────────────┤
│ [صورة] د. سارة   │ أسنان      │ 0523456789  │ 🏢 فرع 2   │ [نشط] │ 2026/1/20     │
└───────────────────────────────────────────────────────────────────────────────────┘
```

## 7.2 نموذج إضافة/تعديل طبيب

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| الاسم الكامل | fullName | text | ✅ |
| التخصص | specialization | text | ✅ |
| رقم الهاتف | phone | tel | ✅ |
| البريد الإلكتروني | email | email | ❌ |
| الفرع | branchId | select | ✅ |
| الحالة | status | select | ✅ |
| ملاحظات | notes | textarea | ❌ |

### Database Schema
```sql
CREATE TABLE doctors (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  specialization VARCHAR(100) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  email VARCHAR(100),
  branch_id UUID REFERENCES branches(id),
  status ENUM('active', 'inactive') DEFAULT 'active',
  notes TEXT,
  avatar_url VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 8️⃣ إدارة المرضى (Patients Management)

## 8.1 صفحة قائمة المرضى

### العنوان
```
إدارة المرضى
إدارة بيانات المرضى وسجلاتهم الطبية
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ إضافة مريض]  [⬇️ تصدير]          🔍 البحث بالاسم أو رقم الهاتف...│
└────────────────────────────────────────────────────────────────┘
```

### بطاقات الإحصائيات
```
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│    👥 6          │  │    ➕ 2          │  │    ✅ 4          │
│  إجمالي المرضى   │  │  المرضى الجدد    │  │  المرضى النشطين  │
└──────────────────┘  └──────────────────┘  └──────────────────┘
```

### جدول المرضى
```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│ المريض              │ رقم الهاتف    │ البريد        │ تاريخ التسجيل │ آخر زيارة    │ ⋮ │
├─────────────────────────────────────────────────────────────────────────────────────┤
│ [مع] محمد عبدالرحمن │ 0512345678   │ m@email.com  │ 2026/2/6     │ 2026/2/6     │ ⋮ │
├─────────────────────────────────────────────────────────────────────────────────────┤
│ [سا] سارة الأحمد    │ 0523456789   │ s@email.com  │ 2026/2/5     │ 2026/2/5     │ ⋮ │
├─────────────────────────────────────────────────────────────────────────────────────┤
│ [خا] خالد العتيبي   │ 0534567890   │ k@email.com  │ 2026/2/4     │ 2026/2/4     │ ⋮ │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### قائمة الإجراءات (⋮ Menu)
```
┌────────────────────┐
│ 👁️ عرض الملف      │
│ ✏️ تعديل          │
│ 📅 إضافة حجز      │
│ 🗑️ حذف           │
└────────────────────┘
```

## 8.2 نموذج إضافة/تعديل مريض

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| الاسم الكامل | fullName | text | ✅ |
| رقم الهاتف | phone | tel | ✅ |
| البريد الإلكتروني | email | email | ❌ |
| تاريخ الميلاد | birthDate | date | ❌ |
| الجنس | gender | select | ❌ |
| العنوان | address | text | ❌ |
| ملاحظات طبية | medicalNotes | textarea | ❌ |
| الفرع | branchId | select | ✅ |

### خيارات الجنس
```javascript
const genderOptions = [
  { value: 'male', label: 'ذكر' },
  { value: 'female', label: 'أنثى' }
];
```

## 8.3 صفحة ملف المريض (Patient Profile)

### الأقسام
```
┌─────────────────────────────────────────────────────────────────┐
│                        ملف المريض                               │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                    البيانات الشخصية                         │ │
│ │ الاسم: محمد عبدالرحمن                                       │ │
│ │ الهاتف: 0512345678                                          │ │
│ │ البريد: m@email.com                                         │ │
│ │ تاريخ الميلاد: 1990/5/15                                    │ │
│ │ الجنس: ذكر                                                  │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                    سجل الحجوزات                            │ │
│ │ [جدول الحجوزات السابقة والقادمة]                            │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                    سجل الفواتير                            │ │
│ │ [جدول الفواتير والمدفوعات]                                  │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### Database Schema
```sql
CREATE TABLE patients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  email VARCHAR(100),
  birth_date DATE,
  gender ENUM('male', 'female'),
  address VARCHAR(255),
  medical_notes TEXT,
  branch_id UUID REFERENCES branches(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 9️⃣ المنتجات والمخزون (Products & Inventory)

## 9.1 صفحة قائمة المنتجات

### العنوان
```
المنتجات والمخزون
إدارة المنتجات والخدمات المتاحة
```

### Tabs
```
┌─────────────┐ ┌─────────────┐
│  المنتجات   │ │  الخدمات    │
└─────────────┘ └─────────────┘
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ إضافة منتج/خدمة]                    🔍 البحث عن منتج...       │
└────────────────────────────────────────────────────────────────┘
```

### جدول المنتجات
```
┌──────────────────────────────────────────────────────────────────────────────────┐
│ المنتج          │ الكود    │ النوع   │ السعر      │ التكلفة   │ الكمية │ الحالة  │
├──────────────────────────────────────────────────────────────────────────────────┤
│ منتج تجريبي 1   │ PRD001  │ منتج   │ 100.00    │ 80.00    │ 50    │ [متوفر] │
├──────────────────────────────────────────────────────────────────────────────────┤
│ خدمة تنظيف     │ SRV001  │ خدمة   │ 150.00    │ -        │ -     │ [نشط]   │
└──────────────────────────────────────────────────────────────────────────────────┘
```

## 9.2 نموذج إضافة/تعديل منتج

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| اسم المنتج | name | text | ✅ |
| الكود | sku | text | ❌ |
| النوع | type | select | ✅ |
| السعر | price | number | ✅ |
| التكلفة | cost | number | ❌ |
| الكمية | quantity | number | ❌ (للمنتجات فقط) |
| الوصف | description | textarea | ❌ |
| الحالة | status | select | ✅ |

### خيارات النوع
```javascript
const typeOptions = [
  { value: 'product', label: 'منتج' },
  { value: 'service', label: 'خدمة' }
];
```

### Database Schema
```sql
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  sku VARCHAR(50),
  type ENUM('product', 'service') NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  cost DECIMAL(10,2),
  quantity INTEGER DEFAULT 0,
  description TEXT,
  status ENUM('active', 'inactive') DEFAULT 'active',
  branch_id UUID REFERENCES branches(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 🔟 الباكجات / الخدمات (Packages)

## 10.1 صفحة قائمة الباكجات

### العنوان
```
الباكجات / الخدمات
إدارة باقات الخدمات والعروض
```

### جدول الباكجات
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ اسم الباكج       │ الوصف              │ السعر      │ الخدمات │ الحالة    │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────┤
│ باقة العناية     │ تنظيف + فحص شامل  │ 500.00    │ 3       │ [نشط]    │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────┤
│ باقة التجميل    │ جلسات تجميل متعددة │ 1200.00   │ 5       │ [نشط]    │ ⋮ │
└──────────────────────────────────────────────────────────────────────────────┘
```

## 10.2 نموذج إضافة/تعديل باكج

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| اسم الباكج | name | text | ✅ |
| الوصف | description | textarea | ❌ |
| السعر | price | number | ✅ |
| الخدمات المشمولة | services | multi-select | ✅ |
| مدة الصلاحية (بالأيام) | validityDays | number | ❌ |
| الحالة | status | select | ✅ |

### Database Schema
```sql
CREATE TABLE packages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  description TEXT,
  price DECIMAL(10,2) NOT NULL,
  validity_days INTEGER,
  status ENUM('active', 'inactive') DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE package_services (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  package_id UUID REFERENCES packages(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id),
  quantity INTEGER DEFAULT 1
);
```

---

# 1️⃣1️⃣ إدارة الحجوزات (Appointments Management)

## 11.1 صفحة الحجوزات

### العنوان
```
إدارة الحجوزات
إدارة المواعيد والحجوزات
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────────────────┐
│ [+ حجز جديد]  [⬇️ تصدير]   │ عرض: [📅 تقويم] [📋 قائمة] │   🔍 بحث...    │
└────────────────────────────────────────────────────────────────────────────┘
```

### فلاتر
```
┌───────┐ ┌───────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ [الكل] │ │ معلق │ │ مؤكد  │ │ مكتمل │ │ ملغي  │
└───────┘ └───────┘ └────────┘ └────────┘ └────────┘
```

### عرض التقويم (Calendar View)
```
┌─────────────────────────────────────────────────────────────────┐
│              📅 فبراير 2026              [< السابق] [التالي >]   │
├─────────────────────────────────────────────────────────────────┤
│  سبت  │  أحد  │ اثنين │ ثلاثاء │ أربعاء │ خميس  │ جمعة  │
├───────┼───────┼───────┼────────┼────────┼───────┼───────┤
│       │   1   │   2   │   3    │   4    │   5   │ [6]   │
│       │  (1)  │       │        │  (1)   │  (2)  │       │
├───────┼───────┼───────┼────────┼────────┼───────┼───────┤
│   7   │   8   │   9   │  10    │  11    │  12   │  13   │
│       │       │       │        │        │       │       │
└───────┴───────┴───────┴────────┴────────┴───────┴───────┘
```

### عرض القائمة (List View)
```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│ المريض           │ الطبيب        │ التاريخ      │ الوقت    │ الفرع    │ الحالة    │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ محمد عبدالرحمن   │ د. أحمد      │ 2026/2/7    │ 10:00   │ فرع 1   │ [مؤكد]   │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ سارة الأحمد      │ د. سارة      │ 2026/2/7    │ 11:30   │ فرع 2   │ [معلق]   │ ⋮ │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

### حالات الحجز مع الألوان
```javascript
const appointmentStatuses = [
  { value: 'pending', label: 'معلق', color: '#FFC107', bgColor: '#FFF8E1' },
  { value: 'confirmed', label: 'مؤكد', color: '#4CAF50', bgColor: '#E8F5E9' },
  { value: 'completed', label: 'مكتمل', color: '#2196F3', bgColor: '#E3F2FD' },
  { value: 'cancelled', label: 'ملغي', color: '#F44336', bgColor: '#FFEBEE' }
];
```

## 11.2 نموذج إضافة/تعديل حجز

### النموذج
```
┌─────────────────────────────────────────────────────────────────┐
│                       حجز جديد                                   │
│                  إضافة موعد جديد للمريض                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  👤 المريض *                                                     │
│  ┌─────────────────────────────────────────────┐                │
│  │ اختر المريض...                        ▼     │                │
│  └─────────────────────────────────────────────┘                │
│  [+ إضافة مريض جديد]                                            │
│                                                                  │
│  🩺 الطبيب *                                                     │
│  ┌─────────────────────────────────────────────┐                │
│  │ اختر الطبيب...                        ▼     │                │
│  └─────────────────────────────────────────────┘                │
│                                                                  │
│  📅 التاريخ *                      🕐 الوقت *                    │
│  ┌──────────────────────┐         ┌──────────────────────┐      │
│  │ 2026/2/7             │         │ 10:00               │      │
│  └──────────────────────┘         └──────────────────────┘      │
│                                                                  │
│  🏢 الفرع *                                                      │
│  ┌─────────────────────────────────────────────┐                │
│  │ اختر الفرع...                         ▼     │                │
│  └─────────────────────────────────────────────┘                │
│                                                                  │
│  📋 نوع الحجز                                                    │
│  ┌─────────────────────────────────────────────┐                │
│  │ اختر النوع...                         ▼     │                │
│  └─────────────────────────────────────────────┘                │
│                                                                  │
│  📝 ملاحظات                                                      │
│  ┌─────────────────────────────────────────────┐                │
│  │                                             │                │
│  │                                             │                │
│  └─────────────────────────────────────────────┘                │
│                                                                  │
│  ┌─────────────────────┐  ┌─────────────────────┐               │
│  │     ✖️ إلغاء        │  │   💾 حفظ الحجز      │               │
│  └─────────────────────┘  └─────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
```

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| المريض | patientId | select/search | ✅ |
| الطبيب | doctorId | select | ✅ |
| التاريخ | date | date | ✅ |
| الوقت | time | time | ✅ |
| الفرع | branchId | select | ✅ |
| نوع الحجز | appointmentType | select | ❌ |
| ملاحظات | notes | textarea | ❌ |
| الحالة | status | select | ✅ |

### خيارات نوع الحجز
```javascript
const appointmentTypes = [
  { value: 'new', label: 'حجز جديد' },
  { value: 'followup', label: 'متابعة' },
  { value: 'consultation', label: 'استشارة' },
  { value: 'emergency', label: 'طوارئ' }
];
```

### Database Schema
```sql
CREATE TABLE appointments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  patient_id UUID REFERENCES patients(id) NOT NULL,
  doctor_id UUID REFERENCES doctors(id) NOT NULL,
  branch_id UUID REFERENCES branches(id) NOT NULL,
  appointment_date DATE NOT NULL,
  appointment_time TIME NOT NULL,
  appointment_type VARCHAR(50),
  status ENUM('pending', 'confirmed', 'completed', 'cancelled') DEFAULT 'pending',
  notes TEXT,
  created_by UUID REFERENCES employees(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 1️⃣2️⃣ إدارة الفواتير (Invoices Management)

## 12.1 صفحة قائمة الفواتير

### العنوان
```
الفواتير
إدارة فواتير العملاء والمبيعات
```

### شريط الأدوات
```
┌────────────────────────────────────────────────────────────────┐
│ [+ فاتورة جديدة]  [⬇️ تصدير]            🔍 بحث برقم الفاتورة...  │
└────────────────────────────────────────────────────────────────┘
```

### فلاتر
```
┌───────┐ ┌─────────┐ ┌───────────┐ ┌───────┐
│ [الكل] │ │ مدفوع  │ │ غير مدفوع │ │ جزئي │
└───────┘ └─────────┘ └───────────┘ └───────┘
```

### جدول الفواتير
```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ رقم الفاتورة │ العميل         │ التاريخ      │ الإجمالي    │ المدفوع    │ الحالة      │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ INV-001     │ محمد عبدالرحمن │ 2026/2/6    │ 500.00     │ 500.00    │ [مدفوع]    │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ INV-002     │ سارة الأحمد    │ 2026/2/5    │ 350.00     │ 200.00    │ [جزئي]     │ ⋮ │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ INV-003     │ خالد العتيبي   │ 2026/2/4    │ 750.00     │ 0.00      │ [غير مدفوع] │ ⋮ │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

### حالات الدفع مع الألوان
```javascript
const paymentStatuses = [
  { value: 'paid', label: 'مدفوع', color: '#4CAF50', bgColor: '#E8F5E9' },
  { value: 'partial', label: 'جزئي', color: '#FFC107', bgColor: '#FFF8E1' },
  { value: 'unpaid', label: 'غير مدفوع', color: '#F44336', bgColor: '#FFEBEE' }
];
```

### قائمة الإجراءات (⋮ Menu)
```
┌────────────────────┐
│ 👁️ عرض            │
│ ✏️ تعديل          │
│ 🖨️ طباعة          │
│ 💰 تسجيل دفعة     │
│ 🗑️ حذف           │
└────────────────────┘
```

## 12.2 نموذج إنشاء فاتورة

### النموذج
```
┌─────────────────────────────────────────────────────────────────────────┐
│                          فاتورة جديدة                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  👤 العميل *                            📅 التاريخ                       │
│  ┌─────────────────────────┐            ┌─────────────────────────┐     │
│  │ اختر العميل...      ▼  │            │ 2026/2/7               │     │
│  └─────────────────────────┘            └─────────────────────────┘     │
│                                                                          │
│  ─────────────────────────────────────────────────────────────────────  │
│                              البنود                                      │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ المنتج/الخدمة    │ الكمية │ السعر    │ الإجمالي │ [🗑️]            │ │
│  ├────────────────────────────────────────────────────────────────────┤ │
│  │ [اختر منتج ▼]    │ [1]   │ 0.00    │ 0.00    │ [🗑️]            │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  [+ إضافة بند]                                                          │
│                                                                          │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                          │
│                                    المجموع الفرعي:     500.00           │
│                                                                          │
│  💯 الخصم (%)     ┌─────────┐            الخصم:        50.00           │
│                   │ 10      │                                           │
│                   └─────────┘                                           │
│                                                                          │
│                                    ═══════════════════════════           │
│                                    الإجمالي:           450.00           │
│                                                                          │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                          │
│  📝 ملاحظات                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                                                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌─────────────────────┐  ┌─────────────────────┐                       │
│  │     ✖️ إلغاء        │  │   💾 حفظ الفاتورة   │                       │
│  └─────────────────────┘  └─────────────────────┘                       │
└─────────────────────────────────────────────────────────────────────────┘
```

### الحقول الرئيسية
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| العميل | patientId | select | ✅ |
| التاريخ | invoiceDate | date | ✅ |
| الخصم | discount | number | ❌ |
| ملاحظات | notes | textarea | ❌ |

### بنود الفاتورة (Invoice Items)
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| المنتج/الخدمة | productId | select | ✅ |
| الكمية | quantity | number | ✅ |
| السعر | unitPrice | number | ✅ |

## 12.3 عرض/طباعة الفاتورة

### تصميم الفاتورة للطباعة
```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│                    [شعار العيادة]                                │
│                    نظام إدارة العيادات                           │
│                                                                  │
│  ═══════════════════════════════════════════════════════════    │
│                          فاتورة                                  │
│  ═══════════════════════════════════════════════════════════    │
│                                                                  │
│  رقم الفاتورة: INV-001              التاريخ: 2026/2/6           │
│  ───────────────────────────────────────────────────────────    │
│                                                                  │
│  العميل: محمد عبدالرحمن                                         │
│  الهاتف: 0512345678                                             │
│                                                                  │
│  ═══════════════════════════════════════════════════════════    │
│  البند              │  الكمية  │  السعر   │  الإجمالي           │
│  ═══════════════════════════════════════════════════════════    │
│  خدمة تنظيف الأسنان │    1    │  200.00  │    200.00           │
│  فحص شامل          │    1    │  150.00  │    150.00           │
│  ───────────────────────────────────────────────────────────    │
│                                                                  │
│                          المجموع الفرعي:      350.00            │
│                          الخصم (10%):         35.00             │
│                          ─────────────────────────────          │
│                          الإجمالي:            315.00            │
│                                                                  │
│  ═══════════════════════════════════════════════════════════    │
│                                                                  │
│                    شكراً لزيارتكم                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Database Schema
```sql
CREATE TABLE invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_number VARCHAR(20) UNIQUE NOT NULL,
  patient_id UUID REFERENCES patients(id) NOT NULL,
  branch_id UUID REFERENCES branches(id) NOT NULL,
  invoice_date DATE NOT NULL,
  subtotal DECIMAL(10,2) NOT NULL,
  discount_percentage DECIMAL(5,2) DEFAULT 0,
  discount_amount DECIMAL(10,2) DEFAULT 0,
  total DECIMAL(10,2) NOT NULL,
  paid_amount DECIMAL(10,2) DEFAULT 0,
  payment_status ENUM('paid', 'partial', 'unpaid') DEFAULT 'unpaid',
  notes TEXT,
  created_by UUID REFERENCES employees(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE invoice_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID REFERENCES invoices(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id),
  quantity INTEGER NOT NULL,
  unit_price DECIMAL(10,2) NOT NULL,
  total DECIMAL(10,2) NOT NULL
);
```

---

# 1️⃣3️⃣ إيصالات العملاء (Customer Receipts)

## 13.1 صفحة قائمة الإيصالات

### العنوان
```
إيصالات العملاء
إدارة إيصالات الدفع والاستلام
```

### جدول الإيصالات
```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│ رقم الإيصال │ العميل         │ رقم الفاتورة │ المبلغ     │ طريقة الدفع │ التاريخ     │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ RCP-001     │ محمد عبدالرحمن │ INV-001     │ 500.00    │ نقدي       │ 2026/2/6   │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ RCP-002     │ سارة الأحمد    │ INV-002     │ 200.00    │ بطاقة      │ 2026/2/5   │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

## 13.2 نموذج إنشاء إيصال

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| الفاتورة | invoiceId | select | ✅ |
| المبلغ | amount | number | ✅ |
| طريقة الدفع | paymentMethod | select | ✅ |
| التاريخ | receiptDate | date | ✅ |
| ملاحظات | notes | textarea | ❌ |

### خيارات طريقة الدفع
```javascript
const paymentMethods = [
  { value: 'cash', label: 'نقدي', icon: '💵' },
  { value: 'card', label: 'بطاقة ائتمان', icon: '💳' },
  { value: 'transfer', label: 'تحويل بنكي', icon: '🏦' },
  { value: 'wallet', label: 'محفظة إلكترونية', icon: '📱' }
];
```

### Database Schema
```sql
CREATE TABLE receipts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  receipt_number VARCHAR(20) UNIQUE NOT NULL,
  invoice_id UUID REFERENCES invoices(id) NOT NULL,
  amount DECIMAL(10,2) NOT NULL,
  payment_method ENUM('cash', 'card', 'transfer', 'wallet') NOT NULL,
  receipt_date DATE NOT NULL,
  notes TEXT,
  created_by UUID REFERENCES employees(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# 1️⃣4️⃣ فواتير المنتجات (Product Invoices)

## 14.1 صفحة قائمة فواتير المنتجات

### العنوان
```
فواتير المنتجات
إدارة فواتير المشتريات والمبيعات للمنتجات
```

### Tabs
```
┌─────────────┐ ┌─────────────┐
│ فواتير شراء │ │ فواتير بيع  │
└─────────────┘ └─────────────┘
```

### جدول الفواتير
```
┌──────────────────────────────────────────────────────────────────────────────────┐
│ رقم الفاتورة │ النوع   │ المورد/العميل  │ التاريخ      │ الإجمالي    │ الحالة    │
├──────────────────────────────────────────────────────────────────────────────────┤
│ PI-001      │ شراء   │ مورد 1         │ 2026/2/5    │ 5000.00    │ [مدفوع]   │
├──────────────────────────────────────────────────────────────────────────────────┤
│ PI-002      │ بيع    │ عميل 1         │ 2026/2/6    │ 1200.00    │ [معلق]    │
└──────────────────────────────────────────────────────────────────────────────────┘
```

### Database Schema
```sql
CREATE TABLE product_invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_number VARCHAR(20) UNIQUE NOT NULL,
  type ENUM('purchase', 'sale') NOT NULL,
  supplier_customer_name VARCHAR(100) NOT NULL,
  invoice_date DATE NOT NULL,
  total DECIMAL(10,2) NOT NULL,
  status ENUM('paid', 'pending', 'cancelled') DEFAULT 'pending',
  notes TEXT,
  branch_id UUID REFERENCES branches(id),
  created_by UUID REFERENCES employees(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_invoice_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID REFERENCES product_invoices(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id),
  quantity INTEGER NOT NULL,
  unit_price DECIMAL(10,2) NOT NULL,
  total DECIMAL(10,2) NOT NULL
);
```

---

# 1️⃣5️⃣ التقارير المالية (Financial Reports)

## 15.1 صفحة التقارير

### العنوان
```
التقارير المالية
عرض وتحليل البيانات المالية
```

### أنواع التقارير المتاحة
```
┌─────────────────────────────────────────────────────────────────┐
│                       التقارير المتاحة                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │ 📊            │  │ 📈            │  │ 📉            │        │
│  │ تقرير المبيعات │  │ تقرير الإيرادات│  │ تقرير المصروفات│        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
│                                                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │ 👥            │  │ 🏢            │  │ 🩺            │        │
│  │ تقرير العملاء │  │ تقرير الفروع  │  │ تقرير الأطباء │        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### فلاتر التقارير
```
┌─────────────────────────────────────────────────────────────────┐
│  📅 من تاريخ              📅 إلى تاريخ              🏢 الفرع    │
│  ┌──────────────┐         ┌──────────────┐         ┌─────────┐ │
│  │ 2026/1/1    │         │ 2026/2/28   │         │ الكل ▼ │ │
│  └──────────────┘         └──────────────┘         └─────────┘ │
│                                                                  │
│                     [🔍 عرض التقرير]                             │
└─────────────────────────────────────────────────────────────────┘
```

### عرض تقرير المبيعات
```
┌─────────────────────────────────────────────────────────────────┐
│                    تقرير المبيعات الشهري                         │
│                  من 2026/2/1 إلى 2026/2/28                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    📊 رسم بياني                          │   │
│  │                                                          │   │
│  │     ▓▓▓▓                                                 │   │
│  │     ▓▓▓▓  ▓▓▓                                            │   │
│  │     ▓▓▓▓  ▓▓▓  ▓▓                                        │   │
│  │  ▓  ▓▓▓▓  ▓▓▓  ▓▓  ▓▓▓▓                                  │   │
│  │  ─────────────────────────                               │   │
│  │  أسبوع1 أسبوع2 أسبوع3 أسبوع4                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ الإحصائيات الملخصة                                       │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ إجمالي المبيعات:        14,500.00                        │   │
│  │ عدد الفواتير:           25                               │   │
│  │ متوسط قيمة الفاتورة:    580.00                           │   │
│  │ أعلى فاتورة:            2,500.00                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────┐  ┌──────────────────────┐             │
│  │    📥 تصدير Excel   │  │    🖨️ طباعة        │             │
│  └──────────────────────┘  └──────────────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

---

# 1️⃣6️⃣ الملف الشخصي (Profile)

## 16.1 صفحة الملف الشخصي

### التصميم
```
┌─────────────────────────────────────────────────────────────────┐
│                       الملف الشخصي                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│              ┌─────────────────┐                                │
│              │    [صورة]       │                                │
│              │   👤 Admin      │                                │
│              └─────────────────┘                                │
│                                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                  │
│  📧 البريد الإلكتروني:     admin@clinic.com                     │
│  📞 رقم الهاتف:            0512345678                           │
│  🏢 الفرع:                 جميع الفروع                          │
│  👔 الدور:                 مدير النظام                          │
│  📅 تاريخ الانضمام:        2026/1/11                            │
│                                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                  │
│  ┌──────────────────────┐  ┌──────────────────────┐             │
│  │   ✏️ تعديل البيانات  │  │  🔐 تغيير كلمة المرور │             │
│  └──────────────────────┘  └──────────────────────┘             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 16.2 نموذج تغيير كلمة المرور

### الحقول
| الحقل | الاسم بالإنجليزية | النوع | مطلوب |
|-------|-------------------|-------|-------|
| كلمة المرور الحالية | currentPassword | password | ✅ |
| كلمة المرور الجديدة | newPassword | password | ✅ |
| تأكيد كلمة المرور | confirmPassword | password | ✅ |

---

# 1️⃣7️⃣ إعدادات النظام (System Settings)

## 17.1 صفحة الإعدادات

### الأقسام
```
┌─────────────────────────────────────────────────────────────────┐
│                      إعدادات النظام                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────┐ │
│  │ الإعدادات   │ │ الفواتير   │ │ الصلاحيات  │ │ النسخ     │ │
│  │ العامة     │ │            │ │            │ │ الاحتياطي │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### قسم الإعدادات العامة
```
┌─────────────────────────────────────────────────────────────────┐
│                     الإعدادات العامة                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🏥 اسم المؤسسة                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ نظام إدارة العيادات المتكامل                             │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  🖼️ شعار المؤسسة                                                │
│  ┌───────────────────────┐                                      │
│  │ [شعار]  [تغيير الشعار] │                                      │
│  └───────────────────────┘                                      │
│                                                                  │
│  💰 العملة                                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ الوقية الموريتانية (MRU)                           ▼    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  🌍 المنطقة الزمنية                                              │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Africa/Nouakchott (GMT+0)                          ▼    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│                     ┌──────────────────────┐                    │
│                     │     💾 حفظ التغييرات │                    │
│                     └──────────────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
```

### Database Schema للإعدادات
```sql
CREATE TABLE settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  key VARCHAR(50) UNIQUE NOT NULL,
  value TEXT,
  type VARCHAR(20), -- 'string', 'number', 'boolean', 'json'
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Default settings
INSERT INTO settings (key, value, type) VALUES
('organization_name', 'نظام إدارة العيادات المتكامل', 'string'),
('logo_url', NULL, 'string'),
('currency', 'MRU', 'string'),
('timezone', 'Africa/Nouakchott', 'string'),
('invoice_prefix', 'INV-', 'string'),
('receipt_prefix', 'RCP-', 'string');
```

---

---

# 🗄️ قاعدة البيانات الكاملة (Complete Database Schema)

## ERD Diagram (Text Representation)
```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   branches   │      │  employees   │      │   doctors    │
├──────────────┤      ├──────────────┤      ├──────────────┤
│ id (PK)      │◄────┤ branch_id    │      │ id (PK)      │
│ name         │      │ id (PK)      │      │ full_name    │
│ address      │      │ full_name    │      │ specialization│
│ phone        │      │ username     │      │ phone        │
│ manager_id   │──────► role         │      │ email        │
│ status       │      │ status       │      │ branch_id    │──►
│ created_at   │      │ created_at   │      │ status       │
└──────────────┘      └──────────────┘      └──────────────┘
        │                    │                      │
        │                    │                      │
        ▼                    ▼                      ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   patients   │      │ appointments │      │   products   │
├──────────────┤      ├──────────────┤      ├──────────────┤
│ id (PK)      │◄────┤ patient_id   │      │ id (PK)      │
│ full_name    │      │ doctor_id    │──────► name         │
│ phone        │      │ branch_id    │      │ sku          │
│ email        │      │ date         │      │ type         │
│ birth_date   │      │ time         │      │ price        │
│ gender       │      │ status       │      │ cost         │
│ branch_id    │──────► notes        │      │ quantity     │
│ created_at   │      │ created_at   │      │ status       │
└──────────────┘      └──────────────┘      └──────────────┘
        │                                          │
        │                                          │
        ▼                                          ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   invoices   │      │invoice_items │      │   packages   │
├──────────────┤      ├──────────────┤      ├──────────────┤
│ id (PK)      │◄────┤ invoice_id   │      │ id (PK)      │
│ invoice_number│      │ product_id   │──────► name         │
│ patient_id   │──────► quantity     │      │ description  │
│ branch_id    │      │ unit_price   │      │ price        │
│ subtotal     │      │ total        │      │ validity_days│
│ discount     │      └──────────────┘      │ status       │
│ total        │                            └──────────────┘
│ paid_amount  │                                   │
│ status       │      ┌──────────────┐            │
│ created_at   │      │package_services│◄─────────┘
└──────────────┘      ├──────────────┤
        │             │ package_id   │
        │             │ product_id   │
        ▼             │ quantity     │
┌──────────────┐      └──────────────┘
│   receipts   │
├──────────────┤      ┌──────────────┐
│ id (PK)      │      │   settings   │
│ receipt_number│      ├──────────────┤
│ invoice_id   │──────► id (PK)      │
│ amount       │      │ key          │
│ payment_method│      │ value        │
│ receipt_date │      │ type         │
│ created_at   │      │ updated_at   │
└──────────────┘      └──────────────┘
```

## Complete SQL Schema
```sql
-- ═══════════════════════════════════════════════════════════════
-- نظام إدارة العيادات المتكامل - Database Schema
-- ═══════════════════════════════════════════════════════════════

-- إنشاء قاعدة البيانات
CREATE DATABASE clinic_management_system;
USE clinic_management_system;

-- ───────────────────────────────────────────────────────────────
-- جدول الفروع (Branches)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE branches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  address VARCHAR(255) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  manager_id UUID,
  status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول الموظفين (Employees)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE employees (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(100),
  password_hash VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  role VARCHAR(20) NOT NULL CHECK (role IN ('ADMIN', 'SALES', 'DOCTOR', 'RECEPTIONIST')),
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL,
  status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  avatar_url VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- تحديث الفروع للإشارة إلى الموظفين
ALTER TABLE branches ADD CONSTRAINT fk_manager 
  FOREIGN KEY (manager_id) REFERENCES employees(id) ON DELETE SET NULL;

-- ───────────────────────────────────────────────────────────────
-- جدول الأطباء (Doctors)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE doctors (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  specialization VARCHAR(100) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  email VARCHAR(100),
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL,
  status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  notes TEXT,
  avatar_url VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول المرضى (Patients)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE patients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  full_name VARCHAR(100) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  email VARCHAR(100),
  birth_date DATE,
  gender VARCHAR(10) CHECK (gender IN ('male', 'female')),
  address VARCHAR(255),
  medical_notes TEXT,
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول المنتجات والخدمات (Products)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  sku VARCHAR(50),
  type VARCHAR(20) NOT NULL CHECK (type IN ('product', 'service')),
  price DECIMAL(10,2) NOT NULL,
  cost DECIMAL(10,2),
  quantity INTEGER DEFAULT 0,
  description TEXT,
  status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول الباكجات (Packages)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE packages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  description TEXT,
  price DECIMAL(10,2) NOT NULL,
  validity_days INTEGER,
  status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول خدمات الباكجات (Package Services)
CREATE TABLE package_services (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  package_id UUID REFERENCES packages(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id) ON DELETE CASCADE,
  quantity INTEGER DEFAULT 1
);

-- ───────────────────────────────────────────────────────────────
-- جدول الحجوزات (Appointments)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE appointments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  patient_id UUID REFERENCES patients(id) ON DELETE CASCADE NOT NULL,
  doctor_id UUID REFERENCES doctors(id) ON DELETE SET NULL NOT NULL,
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL NOT NULL,
  appointment_date DATE NOT NULL,
  appointment_time TIME NOT NULL,
  appointment_type VARCHAR(50),
  status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending', 'confirmed', 'completed', 'cancelled')),
  notes TEXT,
  created_by UUID REFERENCES employees(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول الفواتير (Invoices)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_number VARCHAR(20) UNIQUE NOT NULL,
  patient_id UUID REFERENCES patients(id) ON DELETE SET NULL NOT NULL,
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL NOT NULL,
  invoice_date DATE NOT NULL,
  subtotal DECIMAL(10,2) NOT NULL,
  discount_percentage DECIMAL(5,2) DEFAULT 0,
  discount_amount DECIMAL(10,2) DEFAULT 0,
  total DECIMAL(10,2) NOT NULL,
  paid_amount DECIMAL(10,2) DEFAULT 0,
  payment_status VARCHAR(20) DEFAULT 'unpaid' CHECK (payment_status IN ('paid', 'partial', 'unpaid')),
  notes TEXT,
  created_by UUID REFERENCES employees(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول بنود الفواتير (Invoice Items)
CREATE TABLE invoice_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID REFERENCES invoices(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id) ON DELETE SET NULL,
  quantity INTEGER NOT NULL,
  unit_price DECIMAL(10,2) NOT NULL,
  total DECIMAL(10,2) NOT NULL
);

-- ───────────────────────────────────────────────────────────────
-- جدول الإيصالات (Receipts)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE receipts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  receipt_number VARCHAR(20) UNIQUE NOT NULL,
  invoice_id UUID REFERENCES invoices(id) ON DELETE SET NULL NOT NULL,
  amount DECIMAL(10,2) NOT NULL,
  payment_method VARCHAR(20) NOT NULL CHECK (payment_method IN ('cash', 'card', 'transfer', 'wallet')),
  receipt_date DATE NOT NULL,
  notes TEXT,
  created_by UUID REFERENCES employees(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ───────────────────────────────────────────────────────────────
-- جدول فواتير المنتجات (Product Invoices)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE product_invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_number VARCHAR(20) UNIQUE NOT NULL,
  type VARCHAR(20) NOT NULL CHECK (type IN ('purchase', 'sale')),
  supplier_customer_name VARCHAR(100) NOT NULL,
  invoice_date DATE NOT NULL,
  total DECIMAL(10,2) NOT NULL,
  status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('paid', 'pending', 'cancelled')),
  notes TEXT,
  branch_id UUID REFERENCES branches(id) ON DELETE SET NULL,
  created_by UUID REFERENCES employees(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- جدول بنود فواتير المنتجات (Product Invoice Items)
CREATE TABLE product_invoice_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID REFERENCES product_invoices(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id) ON DELETE SET NULL,
  quantity INTEGER NOT NULL,
  unit_price DECIMAL(10,2) NOT NULL,
  total DECIMAL(10,2) NOT NULL
);

-- ───────────────────────────────────────────────────────────────
-- جدول الإعدادات (Settings)
-- ───────────────────────────────────────────────────────────────
CREATE TABLE settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  key VARCHAR(50) UNIQUE NOT NULL,
  value TEXT,
  type VARCHAR(20) CHECK (type IN ('string', 'number', 'boolean', 'json')),
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- البيانات الافتراضية للإعدادات
INSERT INTO settings (key, value, type) VALUES
('organization_name', 'نظام إدارة العيادات المتكامل', 'string'),
('logo_url', NULL, 'string'),
('currency', 'MRU', 'string'),
('currency_symbol', 'الوقية', 'string'),
('timezone', 'Africa/Nouakchott', 'string'),
('invoice_prefix', 'INV-', 'string'),
('receipt_prefix', 'RCP-', 'string'),
('product_invoice_prefix', 'PI-', 'string');

-- ───────────────────────────────────────────────────────────────
-- Indexes للأداء
-- ───────────────────────────────────────────────────────────────
CREATE INDEX idx_employees_username ON employees(username);
CREATE INDEX idx_employees_branch ON employees(branch_id);
CREATE INDEX idx_patients_phone ON patients(phone);
CREATE INDEX idx_patients_branch ON patients(branch_id);
CREATE INDEX idx_appointments_date ON appointments(appointment_date);
CREATE INDEX idx_appointments_patient ON appointments(patient_id);
CREATE INDEX idx_appointments_doctor ON appointments(doctor_id);
CREATE INDEX idx_invoices_number ON invoices(invoice_number);
CREATE INDEX idx_invoices_patient ON invoices(patient_id);
CREATE INDEX idx_invoices_date ON invoices(invoice_date);
CREATE INDEX idx_receipts_invoice ON receipts(invoice_id);
```

---

# 🔌 API Endpoints الكاملة

## Authentication APIs
```
POST   /api/auth/login                - تسجيل الدخول
POST   /api/auth/logout               - تسجيل الخروج
POST   /api/auth/refresh-token        - تجديد التوكن
GET    /api/auth/me                   - بيانات المستخدم الحالي
PUT    /api/auth/change-password      - تغيير كلمة المرور
```

## Branches APIs
```
GET    /api/branches                  - قائمة الفروع
GET    /api/branches/:id              - فرع واحد
POST   /api/branches                  - إضافة فرع
PUT    /api/branches/:id              - تعديل فرع
DELETE /api/branches/:id              - حذف فرع
GET    /api/branches/:id/stats        - إحصائيات الفرع
```

## Employees APIs
```
GET    /api/employees                 - قائمة الموظفين
GET    /api/employees/:id             - موظف واحد
POST   /api/employees                 - إضافة موظف
PUT    /api/employees/:id             - تعديل موظف
DELETE /api/employees/:id             - حذف موظف
GET    /api/employees/export          - تصدير البيانات
```

## Doctors APIs
```
GET    /api/doctors                   - قائمة الأطباء
GET    /api/doctors/:id               - طبيب واحد
POST   /api/doctors                   - إضافة طبيب
PUT    /api/doctors/:id               - تعديل طبيب
DELETE /api/doctors/:id               - حذف طبيب
GET    /api/doctors/:id/appointments  - حجوزات الطبيب
```

## Patients APIs
```
GET    /api/patients                  - قائمة المرضى
GET    /api/patients/:id              - مريض واحد
POST   /api/patients                  - إضافة مريض
PUT    /api/patients/:id              - تعديل مريض
DELETE /api/patients/:id              - حذف مريض
GET    /api/patients/:id/appointments - حجوزات المريض
GET    /api/patients/:id/invoices     - فواتير المريض
GET    /api/patients/export           - تصدير البيانات
```

## Products APIs
```
GET    /api/products                  - قائمة المنتجات
GET    /api/products/:id              - منتج واحد
POST   /api/products                  - إضافة منتج
PUT    /api/products/:id              - تعديل منتج
DELETE /api/products/:id              - حذف منتج
PUT    /api/products/:id/quantity     - تحديث الكمية
```

## Packages APIs
```
GET    /api/packages                  - قائمة الباكجات
GET    /api/packages/:id              - باكج واحد
POST   /api/packages                  - إضافة باكج
PUT    /api/packages/:id              - تعديل باكج
DELETE /api/packages/:id              - حذف باكج
```

## Appointments APIs
```
GET    /api/appointments              - قائمة الحجوزات
GET    /api/appointments/:id          - حجز واحد
POST   /api/appointments              - إضافة حجز
PUT    /api/appointments/:id          - تعديل حجز
DELETE /api/appointments/:id          - حذف حجز
PUT    /api/appointments/:id/status   - تغيير حالة الحجز
GET    /api/appointments/calendar     - الحجوزات للتقويم
GET    /api/appointments/today        - حجوزات اليوم
```

## Invoices APIs
```
GET    /api/invoices                  - قائمة الفواتير
GET    /api/invoices/:id              - فاتورة واحدة
POST   /api/invoices                  - إنشاء فاتورة
PUT    /api/invoices/:id              - تعديل فاتورة
DELETE /api/invoices/:id              - حذف فاتورة
GET    /api/invoices/:id/print        - طباعة الفاتورة (PDF)
POST   /api/invoices/:id/payment      - تسجيل دفعة
```

## Receipts APIs
```
GET    /api/receipts                  - قائمة الإيصالات
GET    /api/receipts/:id              - إيصال واحد
POST   /api/receipts                  - إنشاء إيصال
DELETE /api/receipts/:id              - حذف إيصال
GET    /api/receipts/:id/print        - طباعة الإيصال
```

## Product Invoices APIs
```
GET    /api/product-invoices          - قائمة فواتير المنتجات
GET    /api/product-invoices/:id      - فاتورة واحدة
POST   /api/product-invoices          - إنشاء فاتورة
PUT    /api/product-invoices/:id      - تعديل فاتورة
DELETE /api/product-invoices/:id      - حذف فاتورة
```

## Reports APIs
```
GET    /api/reports/sales             - تقرير المبيعات
GET    /api/reports/revenue           - تقرير الإيرادات
GET    /api/reports/expenses          - تقرير المصروفات
GET    /api/reports/patients          - تقرير المرضى
GET    /api/reports/branches          - تقرير الفروع
GET    /api/reports/doctors           - تقرير الأطباء
GET    /api/reports/export            - تصدير التقارير
```

## Settings APIs
```
GET    /api/settings                  - جميع الإعدادات
PUT    /api/settings                  - تحديث الإعدادات
POST   /api/settings/logo             - رفع الشعار
```

## Dashboard APIs
```
GET    /api/dashboard/stats           - إحصائيات لوحة التحكم
GET    /api/dashboard/recent-patients - آخر المرضى
GET    /api/dashboard/today-appointments - حجوزات اليوم
GET    /api/dashboard/revenue         - إيرادات الشهر
```

---

# 📦 هيكل المشروع المقترح

```
clinic-management-system/
├── 📁 frontend/
│   ├── 📁 public/
│   │   ├── favicon.ico
│   │   └── logo.png
│   ├── 📁 src/
│   │   ├── 📁 assets/
│   │   │   ├── 📁 icons/
│   │   │   └── 📁 images/
│   │   ├── 📁 components/
│   │   │   ├── 📁 common/
│   │   │   │   ├── Button.tsx
│   │   │   │   ├── Input.tsx
│   │   │   │   ├── Select.tsx
│   │   │   │   ├── Table.tsx
│   │   │   │   ├── Modal.tsx
│   │   │   │   ├── Card.tsx
│   │   │   │   ├── Badge.tsx
│   │   │   │   ├── Loading.tsx
│   │   │   │   └── Pagination.tsx
│   │   │   ├── 📁 layout/
│   │   │   │   ├── Header.tsx
│   │   │   │   ├── Sidebar.tsx
│   │   │   │   ├── Layout.tsx
│   │   │   │   └── Footer.tsx
│   │   │   └── 📁 forms/
│   │   │       ├── BranchForm.tsx
│   │   │       ├── EmployeeForm.tsx
│   │   │       ├── DoctorForm.tsx
│   │   │       ├── PatientForm.tsx
│   │   │       ├── ProductForm.tsx
│   │   │       ├── PackageForm.tsx
│   │   │       ├── AppointmentForm.tsx
│   │   │       ├── InvoiceForm.tsx
│   │   │       └── ReceiptForm.tsx
│   │   ├── 📁 pages/
│   │   │   ├── Login.tsx
│   │   │   ├── Dashboard.tsx
│   │   │   ├── 📁 branches/
│   │   │   │   ├── BranchList.tsx
│   │   │   │   └── BranchDetail.tsx
│   │   │   ├── 📁 employees/
│   │   │   │   ├── EmployeeList.tsx
│   │   │   │   └── EmployeeDetail.tsx
│   │   │   ├── 📁 doctors/
│   │   │   │   ├── DoctorList.tsx
│   │   │   │   └── DoctorDetail.tsx
│   │   │   ├── 📁 patients/
│   │   │   │   ├── PatientList.tsx
│   │   │   │   └── PatientProfile.tsx
│   │   │   ├── 📁 products/
│   │   │   │   └── ProductList.tsx
│   │   │   ├── 📁 packages/
│   │   │   │   └── PackageList.tsx
│   │   │   ├── 📁 appointments/
│   │   │   │   ├── AppointmentList.tsx
│   │   │   │   └── AppointmentCalendar.tsx
│   │   │   ├── 📁 invoices/
│   │   │   │   ├── InvoiceList.tsx
│   │   │   │   ├── InvoiceCreate.tsx
│   │   │   │   └── InvoicePrint.tsx
│   │   │   ├── 📁 receipts/
│   │   │   │   └── ReceiptList.tsx
│   │   │   ├── 📁 product-invoices/
│   │   │   │   └── ProductInvoiceList.tsx
│   │   │   ├── 📁 reports/
│   │   │   │   └── Reports.tsx
│   │   │   ├── Profile.tsx
│   │   │   └── Settings.tsx
│   │   ├── 📁 hooks/
│   │   │   ├── useAuth.ts
│   │   │   ├── useBranches.ts
│   │   │   ├── useEmployees.ts
│   │   │   ├── useDoctors.ts
│   │   │   ├── usePatients.ts
│   │   │   ├── useProducts.ts
│   │   │   ├── usePackages.ts
│   │   │   ├── useAppointments.ts
│   │   │   ├── useInvoices.ts
│   │   │   └── useReceipts.ts
│   │   ├── 📁 services/
│   │   │   ├── api.ts
│   │   │   ├── authService.ts
│   │   │   ├── branchService.ts
│   │   │   ├── employeeService.ts
│   │   │   ├── doctorService.ts
│   │   │   ├── patientService.ts
│   │   │   ├── productService.ts
│   │   │   ├── packageService.ts
│   │   │   ├── appointmentService.ts
│   │   │   ├── invoiceService.ts
│   │   │   └── receiptService.ts
│   │   ├── 📁 store/
│   │   │   ├── index.ts
│   │   │   ├── authSlice.ts
│   │   │   ├── branchSlice.ts
│   │   │   └── settingsSlice.ts
│   │   ├── 📁 types/
│   │   │   ├── auth.ts
│   │   │   ├── branch.ts
│   │   │   ├── employee.ts
│   │   │   ├── doctor.ts
│   │   │   ├── patient.ts
│   │   │   ├── product.ts
│   │   │   ├── package.ts
│   │   │   ├── appointment.ts
│   │   │   ├── invoice.ts
│   │   │   └── receipt.ts
│   │   ├── 📁 utils/
│   │   │   ├── constants.ts
│   │   │   ├── helpers.ts
│   │   │   ├── formatters.ts
│   │   │   └── validators.ts
│   │   ├── 📁 styles/
│   │   │   ├── globals.css
│   │   │   └── themes.css
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── routes.tsx
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   └── vite.config.ts
│
├── 📁 backend/
│   ├── 📁 src/
│   │   ├── 📁 config/
│   │   │   ├── database.ts
│   │   │   └── env.ts
│   │   ├── 📁 controllers/
│   │   │   ├── authController.ts
│   │   │   ├── branchController.ts
│   │   │   ├── employeeController.ts
│   │   │   ├── doctorController.ts
│   │   │   ├── patientController.ts
│   │   │   ├── productController.ts
│   │   │   ├── packageController.ts
│   │   │   ├── appointmentController.ts
│   │   │   ├── invoiceController.ts
│   │   │   ├── receiptController.ts
│   │   │   ├── reportController.ts
│   │   │   └── settingsController.ts
│   │   ├── 📁 middleware/
│   │   │   ├── auth.ts
│   │   │   ├── errorHandler.ts
│   │   │   └── validation.ts
│   │   ├── 📁 models/
│   │   │   ├── Branch.ts
│   │   │   ├── Employee.ts
│   │   │   ├── Doctor.ts
│   │   │   ├── Patient.ts
│   │   │   ├── Product.ts
│   │   │   ├── Package.ts
│   │   │   ├── Appointment.ts
│   │   │   ├── Invoice.ts
│   │   │   ├── Receipt.ts
│   │   │   └── Settings.ts
│   │   ├── 📁 routes/
│   │   │   ├── index.ts
│   │   │   ├── authRoutes.ts
│   │   │   ├── branchRoutes.ts
│   │   │   ├── employeeRoutes.ts
│   │   │   ├── doctorRoutes.ts
│   │   │   ├── patientRoutes.ts
│   │   │   ├── productRoutes.ts
│   │   │   ├── packageRoutes.ts
│   │   │   ├── appointmentRoutes.ts
│   │   │   ├── invoiceRoutes.ts
│   │   │   ├── receiptRoutes.ts
│   │   │   ├── reportRoutes.ts
│   │   │   └── settingsRoutes.ts
│   │   ├── 📁 services/
│   │   │   ├── authService.ts
│   │   │   ├── branchService.ts
│   │   │   ├── employeeService.ts
│   │   │   ├── doctorService.ts
│   │   │   ├── patientService.ts
│   │   │   ├── productService.ts
│   │   │   ├── packageService.ts
│   │   │   ├── appointmentService.ts
│   │   │   ├── invoiceService.ts
│   │   │   ├── receiptService.ts
│   │   │   └── reportService.ts
│   │   ├── 📁 utils/
│   │   │   ├── helpers.ts
│   │   │   ├── jwt.ts
│   │   │   └── validators.ts
│   │   └── app.ts
│   ├── package.json
│   ├── tsconfig.json
│   └── .env.example
│
├── 📁 database/
│   ├── schema.sql
│   ├── seed.sql
│   └── migrations/
│
├── docker-compose.yml
├── README.md
└── .gitignore
```

---

# ✅ قائمة التحقق للبناء (Build Checklist)

## المرحلة 1: الإعداد الأساسي
- [ ] إنشاء مشروع React/Next.js
- [ ] إعداد Tailwind CSS
- [ ] إعداد التوجيه (Routing)
- [ ] إنشاء المكونات الأساسية (Button, Input, etc.)
- [ ] إنشاء Layout Component (Header, Sidebar)
- [ ] إعداد نظام الألوان والخطوط

## المرحلة 2: المصادقة
- [ ] صفحة تسجيل الدخول
- [ ] نظام JWT
- [ ] حماية المسارات
- [ ] إدارة الجلسات

## المرحلة 3: لوحة التحكم
- [ ] بطاقات الإحصائيات
- [ ] الإجراءات السريعة
- [ ] تقويم الحجوزات
- [ ] آخر المرضى

## المرحلة 4: الوحدات الأساسية
- [ ] إدارة الفروع (CRUD)
- [ ] إدارة الموظفين (CRUD)
- [ ] إدارة الأطباء (CRUD)
- [ ] إدارة المرضى (CRUD)

## المرحلة 5: المنتجات والخدمات
- [ ] إدارة المنتجات
- [ ] إدارة الخدمات
- [ ] إدارة الباكجات

## المرحلة 6: الحجوزات
- [ ] إدارة الحجوزات
- [ ] عرض التقويم
- [ ] عرض القائمة
- [ ] تغيير حالة الحجز

## المرحلة 7: الفواتير والمدفوعات
- [ ] إنشاء الفواتير
- [ ] عرض وطباعة الفاتورة
- [ ] إيصالات الدفع
- [ ] فواتير المنتجات

## المرحلة 8: التقارير
- [ ] تقرير المبيعات
- [ ] تقرير الإيرادات
- [ ] تصدير التقارير

## المرحلة 9: الإعدادات
- [ ] الإعدادات العامة
- [ ] إعدادات الفواتير
- [ ] الملف الشخصي

## المرحلة 10: التحسينات
- [ ] الوضع الداكن
- [ ] التصميم المتجاوب
- [ ] تحسين الأداء
- [ ] الاختبارات

---

</div>

© 2026 نظام إدارة العيادات المتكامل - جميع الحقوق محفوظة
